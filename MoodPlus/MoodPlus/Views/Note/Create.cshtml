@using MoodPlus.Data

@{
    ViewData["Title"] = "Create";
}

        @foreach (TempNote note in Model.TempNote)
       {
           <p>@note.q</p>
           <h4>@note.a</h4>
       }




   <div class="form-group">
      <input type="submit" value="Create" class="btn btn-primary" />
   </div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
