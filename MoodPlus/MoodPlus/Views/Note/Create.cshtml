@model IEnumerable<MoodPlus.Models.Note>

@{
    ViewData["Title"] = "Create";
}

        @foreach (var item in Model)
       {
           
       }




   <div class="form-group">
      <input type="submit" value="Create" class="btn btn-primary" />
   </div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
